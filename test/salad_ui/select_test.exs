defmodule SaladUi.SelectTest do
  use ComponentCase

  import SaladUI.Select

  describe "Test select component" do
    test "It renders select_trigger correctly" do
    end

    test "It renders select_content correctly" do
    end

    test "It renders select_group correctly" do
    end

    test "It renders select_label correctly" do
    end

    test "It renders select_item correctly" do
    end

    test "It renders select_separator correctly" do
    end

    test "It renders select correclty" do
    end
  end
end
